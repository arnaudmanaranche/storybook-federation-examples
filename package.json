{
  "private": "true",
  "workspaces": [
    "host",
    "storybook"
  ],
  "scripts": {
    "build": "run-p build:*",
    "build:host": "yarn workspace host build",
    "build:storybook": "yarn workspace storybook build",
    "start": "run-p start:*",
    "start:host": "yarn workspace host start",
    "start:storybook": "yarn workspace storybook start"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "prettier": "2.3.1"
  }
}
